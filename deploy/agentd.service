[Unit]
Description=Heros self-evolving agent daemon
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/local/bin/agentd -config /etc/heros-agent/config.json
Restart=on-failure
User=heros
Group=heros

[Install]
WantedBy=multi-user.target
