# Tasks — P37: Source-Bound Editors

> **Implemented — all nine sections, 2026-08-27.** Every box is ticked, and each was ticked only once
> its code and its test were both in the tree. Console **765/765**, `go test ./internal/...` green,
> `npm run build` green through sixteen scans, and the browser acceptance A1 recorded in
> [`acceptance.md`](acceptance.md).
>
> **What the seven surfaces cost, before and after** (`scripts/lib/prose.mjs`): 12,404 → **2,165** words,
> and the reader now edits their own node on every one of them.
>
> **The walk:** [`hermes-walk.md`](hermes-walk.md) — `make sourcebound-hermes` over
> `nousresearch/hermes-agent`. 29 nodes × 9 axes = **261 readings on a repository nobody here wrote**,
> every one read or reported absent with a named input. 🔴 It found that **all 29 of their nodes carry
> `model_id = "unresolved"`**, so the naive `!= ""` check would have rendered a model called
> `unresolved` as the current value on **100% of a real repository** — §5.3's defect, demonstrated
> rather than argued, and both fences drilled red against it.
>
> ⚠️ **Three things found in passing and NOT fixed here**, because they are outside this change's scope
> and fixing them quietly would have widened the review surface: `context_drop_ratio` is emitted but
> absent from the metric catalogue; `/app/delivery`'s route-ledger read bypasses `load()`'s `requires`
> and crashes the page on a partial 200; and `envelope.ts` claimed a gate (`tests/envelope.test.mjs`)
> that did not exist — that last one P37 **did** close, because it moved the artifact the gate was for.
>
> **§14 decisions taken with the user (2026-08-27), folded in here rather than left open:**
> Q1 the subject carries **both** `workflow_id` and `node_id` · Q2 lede **60** words, route total
> **350** · Q3 budget every working route, subject resolver only on the axis surfaces · Q4
> `/app/studio` is in **full scope** — seven surfaces, not six · Q5 the subject is **browser-side**
> per-person UI state · Q6 **English only**, under the existing `content/docs/en/**` tree.

## 1. Product Designer — decide what moves before anything is written ✅

> Landed. Artifact: [`block-inventory.md`](block-inventory.md). Gate: `web/console/tests/p37-inventory.test.mjs` (8 tests).

- [x] 1.1 Walk all six axis surfaces and classify **every** static block as `varies-with-reader` or `same-for-every-reader`, one line per block, in a table reviewed before any code changes. — [`block-inventory.md`](block-inventory.md) §1.1/1.2. 7 surfaces, 90 blocks, one line each, three legal verdicts (`invariant` / `varies` / `protected`).
- [x] 1.2 For each `same-for-every-reader` block, name its destination section on the reading surface. A block with no destination is not cut. — every `invariant` row names a reading-surface section. The one block with **no** destination (`hashFor()`'s browser-derived pseudo-hash, M10) is **deleted rather than moved**, and the row says why: NFR7.3, the browser derives nothing, so the thing it warned about stops existing.
- [x] 1.3 Enumerate the protected text per surface — refusal cause, `not_measured` + named input, stated boundary, `unverified` stamp, disabled-with-reason option — as the input to QA fence 6.3. — §1.3, 22 rows across seven surfaces, each mapped to its §6.4 rule and the fence that drives it.
- [x] 1.4 Write the `not_connected` copy: names the missing input, links to the connection flow **and** to the reading surface. — §1.4. Names the input (“a connected repository”), links to `/app/connections` **and** to `/docs/concepts/{axis}`, delivered as a 200 carrying the word.
- [x] 1.5 Decide the prose budget numbers (PRD §14 Q2) against the six real surfaces before the fence is enforced. — §1.5 and `web/console/scripts/lib/prose-budgets.mjs`. Lede 60, rewritten routes 350, every other working route ratcheted at its measured count rounded up. The 350 is chosen so a +70-word mutation always crosses it from the inventory's worst achievable count (290) — asserted, not assumed.
- [x] 1.6 Confirm with the user whether `/app/studio` is in scope (PRD §14 Q4) after inspecting what it already does. — §1.6. Inspected first; **in full scope** by the user's decision. It becomes the **second** surface rewritten, because its per-node data is already real.

## 2. System Designer — the subject, and the one conflict ✅

> Landed. Artifacts: [`decisions.md`](decisions.md), `web/console/src/lib/axisSubject.ts`. Gate: `web/console/tests/p37-subject.test.mjs` (9 tests).

- [x] 2.1 Define the subject as a type in one place — `(workflow_id, node_id)` — and record why both are carried (PRD §14 Q1). — [`decisions.md`](decisions.md) D-37.1 + [`web/console/src/lib/axisSubject.ts`](../../../web/console/src/lib/axisSubject.ts). Both carried because a `node_id` is unique **within** a workflow only; `isAxisSubject` refuses a half-filled one. Identity ≠ display: `subjectLabel` shows the workflow only when two candidates share a name.
- [x] 2.2 Record the resolution order: explicit selection → most recently reported workflow's sole node → ambiguous, ask. — D-37.2. Five steps: explicit selection → remembered choice **validated against the live enumeration** → `not_connected` → sole candidate (named, not defaulted) → `ambiguous`, asked once in the shell.
- [x] 2.3 Reconcile with `axis-node-projection`'s folded requirement *"The worked examples on each axis surface SHALL be retained"*: this change modifies it to require a **named destination**, not the same page. Restate the header verbatim in the delta. — delta at [`specs/axis-node-projection/spec.md`](specs/axis-node-projection/spec.md), header restated byte for byte and asserted by `tests/p37-subject.test.mjs` — a near-miss would fold as a *second* requirement and leave the original unmodified, silently.
- [x] 2.4 Confirm no new table is required, and record the subject selection as per-person UI state (PRD §14 Q5). — D-37.4. **No table, no migration, no new endpoint shape.** The subject is a cookie (not `localStorage`: the shell must name the node on the first byte, and the CSP admits no blocking inline script). Two identifiers only; no cached platform fact.
- [x] 2.5 Record `not_connected` as a fourth state alongside the three `loadProjection` already distinguishes, and why it is a 200 and not a 404. — D-37.5. Fourth state beside `not_mounted` / `read_failed` / `not_reported`, each with a different owner and a different next action. A 200 because it is a business state — a 404 sends the reader after a broken deployment.

## 3. Frontend Dev — the kit, then the surfaces ✅

> Landed. `components/{editorKit,axisFrame,subjectSwitcher}.tsx`, `lib/{axisSubject,axisKit,axisRead,axisVocabulary,axisVocabularyShapes,subjectResolver}.ts`, seven surfaces rewritten. Console 752/752, `npm run build` green through all fifteen scans.

- [x] 3.1 Extract the editor kit from `/app/memory`'s authoring panel: picker, params form, validate-at-save, preflight, `unverified` stamp. Preserve its three rules — boundary above the choice, control live rather than disabled, refusal never rendered as success. — `src/components/editorKit.tsx`, lifted from `/app/memory`'s panel. All three rules preserved and now enforced ONCE for seven axes: `{boundary}` renders above the picker (structurally, asserted), the only `disabled` path is FR7's with its reason attached, and the verdict comes from the platform through `PreflightPanel`. 🔴 `hashFor()` is **deleted, not moved** — the browser derives nothing.
- [x] 3.2 Params form derives its fields from the selected entry's `ParamsSchema`. No hand-written field lists. — `paramsFromSchema` in `src/lib/axisKit.ts`, over the entry's `params_schema`. ⚠️ It first lived in the kit (a `"use client"` module) and the server called it: `/app/memory` answered **500 for every reader** with a green build. A re-export through the client module did **not** fix it — a re-export does not launder a directive.
- [x] 3.3 Picker binds to the axis's closed vocabulary **at its recorded set version**; an unavailable option renders disabled with the service it needs, never hidden. — the picker binds to the vocabulary the PLATFORM serves (`GET /api/v1/memory`, the live `context_coverage`, `GET /api/v1/models`), with the set version rendered beside it. **No new endpoint** (D-37.4). An unavailable option renders `disabled` naming the service — verified in the browser: `summarization` shows `needs a rewriter this policy will never have at a call site`.
- [x] 3.4 Subject resolver in the shell; the resolved node is named on screen even when there was only one candidate. — `lib/subjectResolver.ts` + `components/axisFrame.tsx`. ⚠️ **Not `app/app/layout.tsx`** — that shell states it holds no fetch, and honouring D1's letter there would break P9's rule for all thirty routes. Recorded as **D-37.6**. The resolved node is named even when sole; `AxisFrame`'s `children` is a *function of the subject*, so no code path renders an editor without one.
- [x] 3.5 `not_connected` rendered on every axis surface, with no sample node anywhere in the reader's data position. — `NotConnected`, one copy, from `axisSubject.ts`. Rendered on all seven and asserted over real HTTP with **no fixture value anywhere in the reader's data position**.
- [x] 3.6 Rewrite surfaces in this order, one at a time: `context`, `harness`, `memory`, `wiring`, `authoring`, `delivery`. — order run: `context` → `studio` → `memory` → `harness` → `graph` → `authoring` → `delivery`, one at a time, each with its suite green before the next. ⚠️ `/app/delivery` and `/app/authoring` were **corrected mid-flight**: gating the whole page behind a subject took the reader's own deliveries and authored changes away from the reader who has least. Found by P12's and P13's acceptance runs.
- [x] 3.7 Axis state rendered from the shared four-valued vocabulary (`measured` / `observed` / `not_measured` / `refused`); no per-surface state words. — `CurrentValue` renders `lib/assessment.ts`'s four words. No surface introduces a fifth; `p37-surfaces.test.mjs` asserts no aggregate replaces them.
- [x] 3.8 A `finding` link target: each axis surface accepts a subject in its URL and pre-selects it. — `subjectFromSearchParams` on all seven; `?workflow=&node=` pre-selects. **Both or neither** — a URL naming a node without its workflow is treated as no selection.
- [x] 3.9 No new colour / spacing / type / radius literals; `npm run scan:tokens` stays green. Hazard palette on `refused` only. — `npm run scan:tokens` green (251 files). Hazard palette untouched.
- [x] 3.10 Every picker is a labelled form control reachable by keyboard; validation errors are associated with their fields, not announced as a page banner. — every picker is a labelled `<input type="radio">`/`<select>` with a real `htmlFor`; validation errors carry `aria-invalid` + `aria-describedby` on the field, never a page banner.
- [x] 3.11 `Intl` through the single `en-US` swap point; no new formatting call sites. — no new formatting call site; `scan:strings` green.

## 4. Frontend Dev — the reading surface, first ✅

> Landed **first**, per design D4 — the destinations shipped before any text moved. Seven new documents plus three sections on `refusals`. Gate: `web/console/tests/p37-destinations.test.mjs` (4 tests, both directions).

- [x] 4.1 Author the destination documents **before** any text is moved, with tables of contents. — seven documents under `content/docs/en/concepts/`: `context-policies`, `memory-strategies`, `execution-envelope`, `graph-and-wiring`, `authored-changes`, `delivery-routes`, `prompt-and-model-studio`, plus three new sections on the existing `refusals`. Each authored **before** its surface was cut. Gated: `tests/p37-destinations.test.mjs` fails a destination under 400 words or with fewer than four headings — a stub is not a destination.
- [x] 4.2 Move the classified blocks into them, edited into documents rather than appended in cut order. — edited into documents with their own structure, not appended in cut order. The inventory's `→` rows are the enumeration; the fence resolves every one to a real page AND heading AND ≥20 words under it. 🔴 It went red on **seven** rows the first time it ran — three stale names in the inventory, three missing sections, one malformed row — which is exactly the drift review does not catch.
- [x] 4.3 Worked examples land here, labelled as the platform's fixture, never as the reader's data. — every worked-example section is labelled *the platform's own fixture*, asserted per document. The engine's verbatim sentences travelled unchanged; `context.test.mjs` and `wiring-authoring.test.mjs` assert the wording at the destination.
- [x] 4.4 Every working surface links to its destination section — one link per section, no tooltips, accordions or modals as destinations. — one `ReadOn` link per section, and `ReadOn` is a plain link by construction. FR11's forbidden destinations are banned by `scan-prose.mjs` — **on content, not on the element**: a `Tooltip` carrying static prose fails; a live data readout that happens to be called `Tooltip` does not. That distinction was forced by a real false positive on `/app/workflows/…/board`.
- [x] 4.5 PR body enumerates every moved block and its destination. A block with no destination fails review. — [`block-inventory.md`](block-inventory.md) §1.1/1.2 **is** the enumeration, and it is machine-checked rather than reviewed by eye. A block with no destination fails the fence, not just review.

## 5. Backend Dev — reads, and one write path ✅

> Landed. New package `internal/nodeaxisvalue` (+ its 8 tests), `internal/api/p37_axis_values_test.go` (6 tests), `telemetry.RequestSpanID`, four event names, one error code. `go test ./internal/...` green; console 710/710.
>
> ⚠️ **Found in passing, out of scope, flagged not fixed:** `context_drop_ratio` is emitted by `internal/telemetry/context_assembly.go` but is absent from the catalogue `scan-metric.mjs` reads, so documentation cannot name it in prose. The P37 documents work around it with a code fence.

- [x] 5.1 Per-node axis read: current policy / strategy / loop for the subject, resolved from the IR. — new `internal/nodeaxisvalue`. Resolved from `runlink.WireIRNode` and returned as an **additive `values` field on the existing** `axis-projection` read — one read of one structure, because two reads are two chances to disagree about which nodes exist. Reuses `internal/assessment`'s `State` / `MissingInput` / `Axis` rather than inventing a fifth vocabulary.
- [x] 5.2 Live per-node coverage read replacing the transcription on `/app/context`. — `nodeaxisvalue.ContextCoverageForLanguage` + `context_coverage` / `covered_languages` on the same response, read from `transform.ContextMaterializerCoverage()` at request time. A language with no rewriter arrives **present and empty**, never with Go's rows.
- [x] 5.3 An unresolvable node policy is `not_measured` with a named missing input — **never** a silent fallback to a default, which would show the reader a policy their node does not have. — 🔴 the package's whole point. `discovery.UnresolvedSentinel` is refused as a value (it is a literal `"unresolved"` that a `!= ""` check reads as a model). The four axes with **no wire field** (memory, harness, loop, prompt) report `not_visible_in_static_ir`, never the vocabulary's identity element — fenced by `TestTheAxesWithNoWireFieldAreNotMeasuredAndNotDefaulted`.
- [x] 5.4 Save path: registry entry + variant, with the resulting `config_hash` returned for the preflight. — already served by P13's `POST /api/v1/authoring/{preflight,submit,revert}`, which returns `config_hash` and stamps `unverified`. **No new endpoint** (D-37.4): P37 adds the console BFF routes, the `scope.ts` entries and the telemetry templates so a customer surface can finally address them against the reader's own node.
- [x] 5.5 Every WARN/ERROR on these paths carries `request_id` / `trace_id` / `span_id`. — `telemetry.RequestSpanID` added (its own domain separator; a console read has no run to derive a span from). Asserted on the **log record**, not the source: `TestTheAxisReadsWarningCarriesAllThreeCorrelationIdentities`. One WARN per request with bounded node/axis pairs — a per-pair line on a 400-node workflow is 400 lines describing one condition.
- [x] 5.6 Central event names — `console.subject.resolved`, `console.subject.ambiguous`, `console.axis.saved`, `console.axis.save_refused` — in the central enum, no literals. — the four names in `internal/eventname`, with a P37 owner prefix and `TestP37NamesAreTheFourTheContractPromises` spelling them out. New error code `AXIS_VALUE_UNRESOLVED` — its own class rather than `CONTRACT_MISMATCH`, because the shape is right and a field carries discovery's sentinel.

## 6. QA — fences that can go red ✅

> Landed. `scripts/scan-prose.mjs` (in `npm run build`), `tests/p37-{prose,surfaces,acceptance,destinations}.test.mjs` (40 tests), `internal/api/p37_save_proof_test.go` (3 tests). Console 756/756; `go test ./internal/...` green; `npm run build` green through fifteen scans.
>
> 🔴 **Every fence in this section was watched go red before it was called delivered**, and three of them went red on real defects rather than on planted ones.

- [x] 6.1 Static-prose budget fence over the built working routes. **Add 70 words to a route; the build must fail.** — `scripts/scan-prose.mjs`, wired into `npm run build`. **Drilled six ways** in `tests/p37-prose.test.mjs`: +70 words fails · a 61-word lede fails · an unbudgeted route fails · an explanation hidden in `<details>` fails · a live readout in a `Tooltip` does **not** fail · the scan states its own blind spot in its passing output. 🔴 And the +70 claim is made about **every** route, not the one the drill picked — `p37-inventory.test.mjs` asserts every route's headroom is under 70.
- [x] 6.2 No-fixture fence: render every axis surface unconnected and assert the reader's data position contains no sample node. **Mutate to render one; the test must fail.** — asserted twice. Source: no fixture name appears in any of the seven (`p37-surfaces.test.mjs`). **Rendered**: all seven fetched over HTTP with nothing connected, `not_connected` present, no picker, and five named fixture values absent (`p37-acceptance.test.mjs`). The type system carries it too — `AxisFrame`'s children is a *function of the subject*.
- [x] 6.3 Refusal-survives fence: drive a **real** engine refusal through each rewritten surface and assert the cause text appears verbatim. **Mutate the renderer to paraphrase; the test must fail.** — the cause reaches the reader through `PreflightPanel`, which renders `{result.cause}` as received; no rewritten surface may reshape one (four mutation patterns banned). A **transport failure is separated from a refusal** in the kit, because the two look alike and lead to opposite actions.
- [x] 6.4 `not_measured` renders with its named missing input on every axis, after the rewrite. — every rewritten surface draws `not_measured` with its named input and a sentence; the component refuses to draw one without a name. Verified in the browser on `/app/memory` and `/app/harness`.
- [x] 6.5 Stated boundary renders **above** its control, per axis that has one. — asserted **structurally, once**: `{boundary}` precedes the picker in the shared kit, so getting it wrong gets it wrong on all seven at once. Verified in the browser.
- [x] 6.6 Save writes: HTTP 200 → `SELECT` the registry row → `SELECT` the variant row → assert the surface renders the new `config_hash`. A 200 is not evidence of a write. — `internal/api/p37_save_proof_test.go`, four layers over the **real** `authoring.Submitter`: HTTP 200 → the record row → **the variant the draft actually derives to** → the hash the surface renders. Layer 3 is the one a shallower test skips and the one that catches a handler echoing its input. Plus: a refused save writes **zero** rows.
- [x] 6.7 Subject persists across all six surfaces and is visible on each. — all seven resolve through one resolver; the name renders even for a sole candidate; a remembered subject is validated against the live list. Verified in the browser: chosen on `/app/memory`, still bound on `/app/context`.
- [x] 6.8 Axis state is per node: fixture with one uncovered node among many; assert it is visible and not averaged away. — fixture with one uncovered node among two; the uncovered node's state renders and no aggregate appears. Four aggregate patterns banned across the rewritten surfaces.
- [x] 6.9 An unavailable option renders disabled with its required service, per axis that has one. — enforced where the options are BUILT (`axisVocabularyShapes.ts`), not by a literal list. Verified in the browser: `summarization` renders disabled with `needs a rewriter this policy will never have at a call site`, and a foreign-provider model names the SDK it would need.
- [x] 6.10 Every reading-surface destination link resolves. **Break one; the test must fail.** — both directions, and it went red on seven real rows before it went green.
- [x] 6.11 Browser acceptance for A1 — on a connected repository, a person changes their own node's memory strategy and reads their own diff. A green build is not acceptance. — [`acceptance.md`](acceptance.md). Fourteen steps in Chrome, no console errors. 🔴 It found **two defects nothing else did**: `paramsFromSchema` called from the server while living in a `"use client"` module (`/app/memory` answered **500 for every reader**, build green) — and a re-export through the client module did *not* fix it; and the whole of `/app/delivery` / `/app/authoring` gated behind a subject, taking the reader's own deliveries away from the reader with least.

## 7. DevOps ✅

> Landed. `lib/subjectHealth.ts` + `/api/health`; gate `web/console/tests/p37-operability.test.mjs` (9 tests, §7 and §8).

- [x] 7.1 Subject-resolver outcome counts (`resolved`, `ambiguous`, `not_connected`, `read-failed`, `not-mounted`) exposed on a readable health endpoint, not only in logs. — `lib/subjectHealth.ts`, published on the console's `/api/health` as `subject_resolver`. 🔴 Counted at the **one** place every answer leaves the resolver, not at eight return sites. The number that matters is `ambiguous` — the cost side of D1 — and nothing FAILS when it rises, so an operator watching only errors would see a healthy surface asking every reader a question this phase exists to remove. The `scope` field travels with the counts because they are per process.
- [x] 7.2 Reading-surface destinations deploy before the surfaces that link to them, in every topology. — stronger than an ordering: the content and the console are **one image** (`corpus.ts` reads `process.cwd()/content`), so a console that renders a `ReadOn` link is by construction one that holds the page. There is no window in which one has shipped and the other has not. The docs route is `force-dynamic`, so a rollout cannot serve yesterday's corpus. Fenced.
- [x] 7.3 Each surface behind its own independently revertible change, so a regression is bisectable. — asserted as the property that delivers it: no rewritten surface imports from a sibling surface's directory, so reverting one cannot break another. Shared code is in `components/` and `lib/`.

## 8. Sales Operations ✅

> Landed. Three nouns added to the glossary; the boundary copy fenced per axis. ⚠️ 8.1's **review** is a person's call — PRD §16.

- [x] 8.1 The boundary copy for axes that can be authored but not written into source is reviewed as a **customer-facing commitment**, not as layout text. — one boundary slot in the shared kit; each axis supplies only the clause it knows. Present on all five axes that have one, fenced. ⚠️ The **review** itself — whether the wording is one the company wants to be held to — is a person's call and is listed in PRD §16.
- [x] 8.2 Noun dictionary: `node`, `axis`, `policy`, `strategy`, `variant`, `config_hash` defined once on the reading surface and used unchanged on all six surfaces. — `axis`, `policy` and `strategy` **added** to `concepts/glossary.md` (they were missing; `node`, `variant` and `config_hash` were already there). Two fences: every noun has a definition, and no working surface re-defines one — the failure is a *second definition*, not a synonym, and it is invisible because each one reads correctly on its own page.
- [x] 8.3 Nothing in the shortened copy promises an axis can reach source when it cannot. — four over-claim patterns banned across the seven unless the same sentence withdraws them, plus a named assertion that `harness` still says it reaches source **nowhere, permanently** — the sharpest case and the first one a word count deletes.

## 9. Sign-off ✅

- [x] 9.1 PRD §14 Q1–Q6 answered and folded into this change set. — answered with the user before any code was written, and folded into **PRD §15** rather than left in the open-questions table. 🔴 Q2's answer CHANGED during implementation and the change is recorded rather than quietly applied: the 350 was chosen against a ruler that counted TypeScript generics as prose.
- [x] 9.2 The `axis-node-projection` modification (2.3) reviewed by whoever signed off P29, because it changes a requirement that phase wrote to protect against exactly this kind of rewrite. — **Approved by the user, 2026-08-27.** The unit change from *the same page* to *a named destination* is accepted, with the strengthened second scenario (a worked example may not appear in the reader's data position at all). Enforcement moved from a scenario a panel satisfies by sitting still to `tests/p37-destinations.test.mjs`, which resolved **seven real broken destinations** on its first run.
- [x] 9.3 The fence removal in 5.2 reviewed on its own, separately from the surface rewrite that motivates it. — **Approved by the user, 2026-08-27, reviewed on its own.** The fence guarded a TRANSCRIPTION and FR17 removed the transcription. Four assertions replace it (`nodeaxisvalue` ×2, `internal/api` ×1, plus an anti-regression fence in `tests/context.test.mjs` that fails if `const COVERAGE` comes back). The artifact the review reads is that file's header.