# Customer console (P9) — showcased against nousresearch/hermes-agent

Served by `cmd/p9hermes` (platform API :4321) + `web/console` BFF (:4320),
tenant `nousresearch/hermes-agent` via the dev identity provider.
Each `*.txt` here is the server-rendered page text captured live from the running console.
